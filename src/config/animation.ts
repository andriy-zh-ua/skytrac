// Animation configuration
export const animationConfig = {
  pulseInterval: 500, // ms between pulses
  pulseDuration: 2000, // ms for pulse to reach partition
};

export type AnimationConfig = typeof animationConfig;
