// Animation Configuration
export const ANIMATION_CONFIG = {
  // Producer edge animation speed in milliseconds
  // Lower values = faster animation, higher values = slower animation
  producerEdgeAnimationSpeed: 500,
  
  // Available speed presets (uncomment to use):
  // Very Fast: 200ms (5 cycles per second)
  // Fast: 300ms (3.3 cycles per second)
  // Fast+: 400ms (2.5 cycles per second)
  // Normal: 500ms (2 cycles per second)
  // Slow: 600ms (1.7 cycles per second)
  // Slow+: 800ms (1.25 cycles per second)
  // Very Slow: 1000ms (1 cycle per second)
  // Ultra Slow: 1500ms (0.67 cycles per second)
  // Slowest: 2000ms (0.5 cycles per second)
};
